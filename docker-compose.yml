version: "3.9"
services:
  app:
    image: my-node-service:local
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - app/.env         # DO NOT COMMIT this file
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
